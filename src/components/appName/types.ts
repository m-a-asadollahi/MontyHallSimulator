//* Type of Name component's props
export interface NamePropsType {
  name: string;
}

//* Type of Name component's style
export interface StyledNameType {
  textColor: string;
  textShadowColor: string;
}
